#include <stdio.h>
#include <stdlib.h>

#include "chess.h"

Game* readGame(char* file_name) {

    return NULL;
}

Game* writeGame(Game* game) {

    return NULL;
}

Move* getMoves(Game* game) {

    return NULL;
}


char* gameHash(Game* game) {

    return NULL;
}
